# Core runtime: API, server, validation, multipart uploads.
# Tests run against the mock engine, so this is all they need.
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
pydantic>=2.7,<3.0
python-multipart>=0.0.9
